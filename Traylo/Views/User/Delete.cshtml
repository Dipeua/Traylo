@model User

@{
    ViewData["Title"] = "Supprimer le gerant";
}

<div class="container-fluid">
    <div tabindex="-1" role="dialog" aria-labelledby="addUserModalLabel">
        <div>
            <div>
                <form method="post">
                    <div>
                        <h5 class="modal-title">Supprimer le gerant</h5>
                        <button type="button" class="close text-white">
                            <span>&times;</span>
                        </button>
                    </div>
                    <div>
                        <div class="form-group">
                            <label asp-for="Username"></label>
                            <input asp-for="Username" class="form-control" placeholder="Nom du gerant" required disabled>
                        </div>
                    </div>
                    <div>
                        <a type="button" class="btn btn-secondary" asp-controller="User" asp-action="Index">Annuler</a>
                        <button type="submit" class="btn btn-danger">Supprimer</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>